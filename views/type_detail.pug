extends layout 

block content 
  h1 Type #{typing.name} 

  div 
    h4 Pokemon
    dl 
      each poke in pokemon
        dt 
          a(href=poke.url) #{poke.name}

      else 
        p There are no Pokemon in this Type

  hr
  p
    a(href=typing.url+'/delete') Delete Type
  p
    a(href=typing.url+'/update') Update Type